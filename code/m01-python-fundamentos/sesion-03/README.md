# S03 — Código de la sesión

```bash
uv run python main.py
```

`main.py` recorre las cuatro estructuras (`list`, `tuple`, `dict`, `set`), demuestra el gotcha de mutabilidad y termina con un catálogo de TiendaPro como lista-de-diccionarios.

Después de ejecutarlo, modifícalo: agrega productos, cambia los criterios de orden, prueba qué pasa cuando comparás dos listas con `==` vs `is`.
